apply plugin: 'java'
apply plugin: 'org.springframework.boot'

jar {  
    baseName = 'Oripyon_jr'
    version = '0.0.1-SNAPSHOT'

    // To include resources from webapp
    from 'src/main/java/com/bot'
}

findbugs {
	ignoreFailures = true
}

dependencies {
    compile project(':line-bot-spring-boot')
}
